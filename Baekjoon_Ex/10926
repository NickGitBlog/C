//#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
#include <string.h>

int main(){
	char id[30];
	
	fgets(id, 30, stdin);
	
	size_t len = strlen(id);

	if (id[len - 1] == '\n')
		id[len - 1] = '\0';

	printf("%s??!", id);
	return 0;
}
