#include <stdio.h>

int main()
{
int n, m;
scanf("%d %d", &n, &m);
int arr[101] = { 0, };
for (int i = 1; i <= m; i++)
{
int a, b, c;
scanf("%d %d %d", &a, &b, &c);
for (int j = a; j <= b; j++)
{
arr[j] = c;
}
}
for (int i = 1; i <= n; i++)
{
printf("%d ", arr[i]);
}
}
