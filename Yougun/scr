_CRT_SECURE_NO_WARNINGS
#include<stdio.h>

int main(void) {
	int scr = 0;
	scanf("%d", &scr);

	if (scr >= 90 )
		printf("A");

	else if (scr >= 80 )
		printf("B");

	else if (scr >= 70 )
		printf("C");

	else if (scr >= 60 )
		printf("D");

	else
		printf("F");
}
