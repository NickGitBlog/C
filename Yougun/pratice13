#define _CRT_SECURE_NO_WARNINGS
#include<stdio.h>
#include<string.h>

int main(void) {
	char str[100] = "\0";
	char a, z;
	int len = 0;
	scanf("%d");

	for (int i = 0; i < 200; i++);
	{
		scanf("%s", str);

		len = strlen(str);
		a = str[0];
		z = str[len - 1];
		printf("%c,%c\n", a, z);
	}
}
